{
  "name": "unified-cli-browser-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Opt-in browser regression harness; never a runtime dependency.",
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "axe-core": "4.12.1"
  },
  "scripts": {
    "test": "playwright test",
    "install:browsers": "playwright install --with-deps chromium"
  }
}
